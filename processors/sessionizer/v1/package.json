{
  "name": "@polaris/processor-sessionizer-v1",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Polaris sessionizer processor v1. Consumes raw.events, maintains an in-memory inactivity window per (project_id, environment, primary_identifier), and emits session.started / session.ended events on session.events. SDK session_id is treated as a hint, not authoritative.",
  "license": "UNLICENSED",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "src",
    "processor.manifest.yaml",
    "CHANGELOG.md"
  ],
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist coverage",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "biome lint src test",
    "format": "biome format --write src test",
    "test": "vitest run",
    "test:watch": "vitest",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@polaris/shared-config": "workspace:*",
    "@polaris/shared-kafka": "workspace:*",
    "@polaris/shared-logger": "workspace:*",
    "@polaris/shared-metrics": "workspace:*",
    "@polaris/shared-processor": "workspace:*",
    "@polaris/shared-schemas": "workspace:*",
    "@polaris/shared-service-bootstrap": "workspace:*",
    "kafkajs": "^2.2.4",
    "uuid": "^14.0.0",
    "zod": "^4.4.3"
  }
}
