{
  "name": "@polaris/auth",
  "version": "0.0.0",
  "private": true,
  "description": "Idp access-token verification for Polaris services. A vendored, trimmed subset of the private @idp/jwt package (cassiopagnoncelli/idp-js), carrying only what a resource server needs: JWKS-backed ES256 verification, the Passport claim accessors, and the refresh-token grant. See README.md for provenance and the upgrade procedure.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist coverage .tsbuildinfo",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "biome lint src test",
    "format": "biome format --write src test",
    "test": "vitest run --config vitest.config.ts",
    "test:watch": "vitest --config vitest.config.ts"
  },
  "dependencies": {
    "jose": "^6.2.2"
  }
}
