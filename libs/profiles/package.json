{
  "name": "@polaris/profiles",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The profile aggregate: the person Polaris resolves an event to. Identifier bindings (external IDs), the trait model and its merge-patch rule, and the resolution result the identity stage stamps onto the spine. Pure — no clock, no I/O, no database.",
  "license": "UNLICENSED",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist coverage",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "biome lint src test",
    "format": "biome format --write src test",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@polaris/identity-merge": "workspace:*",
    "@polaris/identity-rules": "workspace:*"
  }
}
