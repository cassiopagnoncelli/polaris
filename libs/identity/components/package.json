{
  "name": "@polaris/identity-components",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Incremental connected components over merged profiles: a disjoint-set forest kept fully path-compressed, so asking who a tombstoned profile became is always one lookup. Pure — no clock, no I/O, no database.",
  "license": "UNLICENSED",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist coverage",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "biome lint src test",
    "format": "biome format --write src test",
    "test": "vitest run",
    "test:watch": "vitest"
  }
}
