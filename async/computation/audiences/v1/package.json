{
  "name": "@polaris/processor-audiences-v1",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Polaris audiences runner v1. Evaluates the definitions in catalog/audiences/ against computed traits (or a sanctioned projection), diffs the resulting population against audience_memberships, and emits audience.entered / audience.exited on profile.events for transitions only. No long-lived service: `polaris audiences compute` is the entrypoint.",
  "license": "UNLICENSED",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "src",
    "CHANGELOG.md"
  ],
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist coverage",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "biome lint src test",
    "format": "biome format --write src test",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@polaris/audience-catalog": "workspace:*"
  }
}
