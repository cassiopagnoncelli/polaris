# Polaris processor manifest — sessionizer v2
#
# This manifest is the SEMANTIC definition for the sessionizer
# processor, version 1. Per docs/architecture/05-processors-and-replay.md
# ("Processor Versioning"), the contents of this file for a given
# (name, version) pair are immutable. Any change that affects emitted
# event meaning, fields, identity links, attribution outcomes, filtering
# behavior, or output schema requires a NEW version directory (v2/) with
# its own manifest.
#
# PostgreSQL stores the runtime activation state (enabled/disabled,
# project/environment scope, consumer group, batch size, ...) — never
# semantic transformation rules. See docs/architecture/05-processors-and-replay.md
# "Processor Configuration".

name: sessionizer
version: v2
owner: platform-data
# P8-006: closed-set release lifecycle flag. `released` means this version
# is the production semantics; any output-changing fix is a new version
# (v2/) per docs/architecture/05-processors-and-replay.md "Processor
# Versioning". See docs/development/processor-manifests.md.
release_status: released
description: |
  Sessionization keyed on the PERSON. Consumes `resolved.events` and
  maintains an inactivity window per
  `(project_id, environment, profile_id)` in Redis.

  The one thing that changed from v1, and the reason this is a new
  major: v1 keyed on whichever identifier an event happened to carry,
  walking customer_id > anonymous_id > session_id. So a visitor browsing
  anonymously keyed on `anonymous_id`, and the moment they logged in the
  same person began keying on `customer_id` — the pre-login window was
  orphaned against a key nobody would touch again, and a second session
  opened for someone who never left. Every funnel spanning a login
  measured two sessions where there was one.

  The identity stage answers "who is this?" upstream now, so v2 reads
  `profile.profile_id` and nothing else. Events the identity stage could
  not resolve to a person carry `profile: null` and are DROPPED, exactly
  as v1 dropped events carrying none of its three identifiers.

  Accepted limitation — merge-moment rotation. When two profiles merge,
  the loser's identifiers repoint to the winner, so events that were
  keying on the loser's id begin keying on the winner's. The window open
  under the loser's key is orphaned and expires unclosed. This is the
  same class of gap v1 had at every login, reduced to the rare case of
  an actual merge, and it is not fixable inside a keyed-window design:
  the sessionizer learns about the merge only by seeing the new key.
  Rebuilding sessions after a merge storm is a replay, not a repair.

  SDK behavior reference: the Web SDK rotates `session_id` after 30
  minutes of inactivity per `docs/architecture/10-sdk-standards.md`.
  This processor treats SDK `session_id` as a HINT only — its own
  `session_id` is deterministically derived from
  `(profile_id, started_at)`, domain-separated from v1's derivation so
  the two versions running side by side never mint the same id for
  windows that mean different things.

  Outputs:
    - `session.started` — emitted the first time a key opens a window
      (or after lazy expiration on the next event for that key).
    - `session.ended`   — emitted when the next event for a key arrives
      after `inactivity_seconds` of silence; `ended_at` is anchored to
      the window boundary (`last_seen_at + inactivity_seconds`) so the
      timeline is stable across replays.

  Campaign-change handling: unchanged from v1. Campaign attribution is
  captured in context, not used to rotate sessions.

  State store: Redis, with the inactivity window as the key TTL. A Redis
  outage is FAIL-CLOSED — the processor stops rather than silently
  opening duplicate windows against a store that forgot the old ones.

# Streaming mode: each input event maps to zero, one, or two emitted
# session.* events (start; or end-then-start on lazy expiration).
mode: streaming

# Topic-family inputs/outputs. The shared-kafka topic-family resolver
# turns these into concrete topic names per project isolation state at
# runtime. Per-project activation lives in PostgreSQL (P6-005).
inputs:
  - family: resolved.events
    schema_versions: "*"   # the sessionizer does not gate on event schema version
outputs:
  - family: session.events
    schema_versions: [1]

# State stores this processor uses.
state_stores:
  # Redis, not memory. v1's manifest said `memory:sessions` and shipped a
  # Redis store — the sessions live in Redis with the inactivity window as
  # the key TTL, which is why a Redis outage is a fail-closed condition
  # rather than a degraded one. A manifest that misdescribes where state
  # lives sends an incident responder to the wrong system.
  - redis:sessions

# Operational defaults the runtime helpers fall back to when no
# PostgreSQL activation row overrides them. Non-semantic per the
# architecture doc ("Processor Configuration") EXCEPT for
# `session.inactivity_seconds`, which IS semantic: changing it would
# alter emitted `session.started` / `session.ended` events. Per the
# architecture rule "If changing a setting can change emitted event
# meaning, the setting is semantic", the inactivity window MUST live
# in the manifest, not in env. A bump requires v2.
defaults:
  consumer_group: polaris-sessionizer-v2
  # SEMANTIC: 30 minutes per docs/architecture/10-sdk-standards.md and
  # the Web SDK's session rotation rule. v1 emits session.ended for any
  # key with last_seen_at + 1800s < occurred_at on the next observed
  # event. Changing this value requires a new processor version.
  session_inactivity_seconds: 1800

# Replay metadata. The sessionizer is replayable from raw.events; the
# deterministic session_id derivation guarantees the same output for
# the same input slice.
replay:
  supported: true
  restrictions: []

# P8-006: free-form replay notes for operators.
replay_notes: |
  The sessionizer is fully replayable from raw.events. session_id derives
  deterministically from (primary_identifier_kind, primary_identifier_value,
  started_at_iso) via SHA-256, so the same input slice produces byte-
  identical session.started / session.ended events on replay. The
  in-memory state store is rebuilt FROM THE BEGINNING of the replay slice.
  A replay that starts AFTER an identifier's original session.started will
  emit a NEW session.started for whichever event first crosses the
  inactivity boundary in the slice. Downstream analytics should join
  session events back to raw events on (project_id, environment,
  primary_identifier, occurred_at) so the join is stable across replays.

# P8-006: golden fixture pairs.
fixtures:
  - name: session-started-for-a-profile
    input: test/golden/session-started-for-a-profile.input.json
    output: test/golden/session-started-for-a-profile.output.json
    description: |
      First event of a new window for a person. Exercises the
      session.started emission, the deterministic session_id derivation
      from `(profile_id, started_at)`, and the store key
      `<project>::<environment>::<profile_id>`.

      Note what the input carries: an `anonymous_id` and NO
      `customer_id`, plus a `profile` block. v1 would have keyed this on
      the anonymous id and re-keyed the moment a customer id appeared.
      v2 keys on the profile, so the same window survives the login —
      which is the whole reason for the major version.
