# P0-008: Shared Secrets Package

Status: Backlog

## Goal

Create the provider-based secret reference abstraction.

## Required Reading

- [Control Plane](../../architecture/02-control-plane.md)
- [Destinations](../../architecture/06-destinations.md)
- [Engineering Standards](../../architecture/09-engineering-standards.md)

## Dependencies

- P0-003
- P0-004

## Write Scope

Allowed:

```text
packages/shared-secrets/
package.json
pnpm-workspace.yaml
```

Forbidden:

```text
apps/
processors/
consumers/
```

## Implementation Notes

- Implement an `env` provider first.
- Define interfaces for future external secret providers.
- Never log secret values.
- Tests should prove missing secrets fail clearly and values are not exposed by diagnostics.

## Acceptance Criteria

- [ ] Package exports secret provider interface.
- [ ] Package exports env provider.
- [ ] Tests cover successful lookup and missing secret.
- [ ] Secret values are not included in error strings.

## Checks

Run where possible:

```text
pnpm typecheck
pnpm test
pnpm lint
```

## Handoff

```text
Files changed:
  packages/shared-secrets/package.json
  packages/shared-secrets/tsconfig.json
  packages/shared-secrets/vitest.config.ts
  packages/shared-secrets/src/index.ts
  packages/shared-secrets/src/types.ts
  packages/shared-secrets/src/errors.ts
  packages/shared-secrets/src/reference.ts
  packages/shared-secrets/src/resolver.ts
  packages/shared-secrets/src/providers/index.ts
  packages/shared-secrets/src/providers/env.ts
  packages/shared-secrets/test/reference.test.ts
  packages/shared-secrets/test/env-provider.test.ts
  packages/shared-secrets/test/resolver.test.ts
  packages/shared-secrets/test/errors.test.ts
  pnpm-lock.yaml (regenerated by pnpm install)

Commands run:
  pnpm install
  pnpm --filter @polaris/shared-config build
  pnpm --filter @polaris/shared-logger build
  pnpm typecheck
  pnpm lint
  pnpm format:check
  pnpm --filter @polaris/shared-secrets format
  pnpm test
  pnpm --filter @polaris/shared-secrets build

Checks passed:
  pnpm typecheck (all 5 workspace projects clean)
  pnpm format:check (clean across 74 files)
  pnpm lint (no new diagnostics; only pre-existing warnings in shared-db
    and shared-logger remain — none from this package)
  pnpm test (206 tests pass; 48 new tests across reference, env-provider,
    resolver, errors)
  pnpm --filter @polaris/shared-secrets build (emits dist/ with d.ts maps)

Known gaps:
  - Vault adapter lands in P11-004 through the same interface; provider
    enum slots for `vault`, `aws-secrets-manager`, `gcp-secret-manager`,
    and `azure-keyvault` are reserved but resolve through
    SecretProviderNotConfiguredError until adapters are wired.
  - No README; package docstring in src/index.ts covers usage. Add a
    README if the adopting service tasks (P9-*) request one.
  - shared-config / shared-logger consumers must build before this
    package typechecks fresh (standard workspace order; pnpm -r handles it).
```

