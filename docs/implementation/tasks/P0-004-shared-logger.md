# P0-004: Shared Logger Package

Status: Done

## Goal

Create a shared Pino logger package with standard context fields and redaction.

## Required Reading

- [Engineering Standards](../../architecture/09-engineering-standards.md)
- [Observability and Operations](../../architecture/08-observability-and-operations.md)
- [Claude Instructions](../../instructions/claude.md)

## Dependencies

- P0-001
- P0-002

## Write Scope

Allowed:

```text
packages/shared-logger/
package.json
pnpm-workspace.yaml
```

Forbidden:

```text
apps/
processors/
consumers/
packages/shared-config/
packages/shared-kafka/
```

## Implementation Notes

- Use Pino.
- JSON logs only.
- Redact secrets, authorization headers, cookies, tokens, card fields, and passwords.
- Do not log raw event payloads by default.

## Acceptance Criteria

- [ ] `packages/shared-logger` exists.
- [ ] It exports logger creation helpers.
- [ ] Redaction defaults are covered by tests.
- [ ] Child logger/context usage is documented in package README or source comments.

## Checks

Run where possible:

```text
pnpm typecheck
pnpm test
pnpm lint
```

## Handoff

```text
Files changed:
  packages/shared-logger/.gitignore                    (new)
  packages/shared-logger/README.md                     (new)
  packages/shared-logger/biome.json                    (new)
  packages/shared-logger/package.json                  (new)
  packages/shared-logger/tsconfig.json                 (new)
  packages/shared-logger/tsconfig.test.json            (new)
  packages/shared-logger/vitest.config.ts              (new)
  packages/shared-logger/src/index.ts                  (new)
  packages/shared-logger/src/logger.ts                 (new)
  packages/shared-logger/src/redaction.ts              (new)
  packages/shared-logger/src/context.ts                (new)
  packages/shared-logger/src/types.ts                  (new)
  packages/shared-logger/test/redaction.test.ts        (new)
  packages/shared-logger/test/logger.test.ts           (new)
  packages/shared-logger/test/context.test.ts          (new)
  docs/implementation/tasks/P0-004-shared-logger.md    (handoff update only)

Commands run:
  pnpm install
  pnpm --filter @polaris/shared-logger typecheck
  pnpm --filter @polaris/shared-logger lint
  pnpm --filter @polaris/shared-logger test
  pnpm --filter @polaris/shared-logger build
  pnpm typecheck
  pnpm lint
  pnpm test
  pnpm build

Checks passed:
  pnpm typecheck                                 (workspace)
  pnpm lint                                      (workspace, Biome 2.4)
  pnpm test                                      (Vitest, 68 tests across 3 files)
  pnpm build                                     (workspace, tsc emit)
  pnpm --filter @polaris/shared-logger build     (dist/ emitted with .d.ts + sourcemaps)

Known gaps:
  - Worker assumption from instructions vs. worktree reality: the prompt
    stated P0-002 (TypeScript Tooling Baseline) had landed and that the
    root would already have tsconfig.base.json, tsconfig.json, biome.json,
    and vitest.config.ts. None of those exist on this branch
    (worktree-agent-ae4ccc66e7fe54c68 was cut after P0-001 / P1-001 /
    P1-003, before P0-002). Rather than touch root tooling outside the
    P0-004 write scope, this package ships a self-contained tsconfig
    chain, biome.json, and vitest.config.ts so its checks run locally.
    When P0-002 lands, the package's tsconfig.json may be adjusted to
    `extends: "../../tsconfig.base.json"` and the package-local biome.json
    can be dropped if the root config covers the same rules. The package
    deps (pino, typescript, vitest, biome, @types/node) were added to the
    package's own package.json, not the root.
  - pnpm-lock.yaml was generated by `pnpm install`. Per the prompt,
    integration regenerates the lockfile, so it is left uncommitted in
    the worktree.
  - The root .gitignore is a single-line file (only `resources/maxmind`).
    Editing it was out of P0-004 scope, so a package-level .gitignore
    inside packages/shared-logger/ covers dist/, coverage/, and
    node_modules/. P0-002 should extend the root .gitignore when it lands.
  - Logger redaction is the second line of defense. Pattern-based
    (high-entropy) detection lives in the forbidden-field policy
    evaluator (P0-009 / shared-policy), which runs before any log line is
    emitted. This package handles only field-name-based redaction in
    structured log bindings.
```

