# P12-002: API Docs and OpenAPI Publishing

Status: Done (merged in `b27d15e`)

## Goal

Publish usable API documentation for the ingester and any control-plane/admin HTTP APIs.

## Required Reading

- [Engineering Standards](../../architecture/09-engineering-standards.md)
- [Ingestion and SDKs](../../architecture/04-ingestion-and-sdks.md)
- [Event Contract](../../architecture/01-event-contract.md)

## Dependencies

- P2-003
- P6-001

## Write Scope

Allowed:

```text
docs/api/
apps/ingester-api/
apps/*/
scripts/
```

Forbidden:

```text
event schema semantic changes unless correcting docs drift
```

## Implementation Notes

- OpenAPI should be generated from route schemas.
- Do not hand-write specs that drift from implementation.
- Include examples for batch partial acceptance and Problem Details errors.

## Acceptance Criteria

- [ ] OpenAPI generation command exists.
- [ ] Generated spec is committed or documented according to repo policy.
- [ ] API docs include auth, batch ingestion, error format, and examples.
- [ ] Docs link to SDK handbook.

## Checks

Run where possible:

```text
pnpm openapi
```

## Handoff

```text
Files changed:
  Added:
    apps/ingester-api/src/openapi/
      document.ts        builds the OpenAPI 3.0 document; derives schemas from the Zod
                         sources in @polaris/shared-schemas via z.toJSONSchema with the
                         "openapi-3.0" target.
      paths.ts           POST /v1/events operation, /health, /ready, /metrics; documents
                         every documented Problem code (400, 401, 413, 415, 500, 503) with
                         examples, and partial-accept batch response examples.
      route.ts           openApiSetup hook implementing @polaris/shared-service-bootstrap's
                         OpenApiSetup type; registers GET /openapi.json.
      schemas.ts         Zod-to-JSON-Schema conversion for Envelope, ProducerEnvelope,
                         BatchRequest/Response, reason-code enums, plus a hand-written
                         ProblemBody schema.
      index.ts           barrel.
    apps/ingester-api/test/openapi.test.ts     15 tests covering document shape,
                                               Problem responses, deterministic
                                               build, live /openapi.json route, and
                                               the NOOP_OPENAPI_SETUP escape hatch.
    scripts/openapi-generate.mjs               generator + --check drift mode.
    scripts/__tests__/openapi-generate.test.ts CI drift test (3 tests): regenerates
                                               byte-identical YAML and JSON to the
                                               committed copies; asserts serializer
                                               determinism.
    docs/api/README.md                         operator-facing entry; canonical YAML
                                               location, rendering instructions
                                               (Redocly, Swagger UI container),
                                               regeneration commands, pointers to
                                               SDK handbook + architecture docs.
    docs/api/openapi.yaml                      generated, committed (1140 lines).
    docs/api/openapi.json                      generated, committed.

  Modified:
    apps/ingester-api/src/app.ts        installs the openApiSetup by default
                                         (replaces NOOP_OPENAPI_SETUP).
    apps/ingester-api/src/index.ts      barrel exports the openapi surface.
    package.json                        adds `openapi`, `openapi:check` scripts and
                                         `yaml` devDep (already a transitive dep;
                                         pinned at root for the script).
    biome.json                          excludes docs/api/openapi.{yaml,json} from
                                         formatting (generated files).
    pnpm-lock.yaml                      regenerated by pnpm install.

Commands run:
  pnpm install
  pnpm build
  pnpm typecheck
  pnpm lint                              (Biome + ClickHouse import-restriction)
  pnpm format:check
  pnpm test                              893 workspace tests + 21 script tests pass
  pnpm --filter @polaris/ingester-api run test    101 tests
  pnpm openapi                           wrote docs/api/openapi.{yaml,json}
  pnpm openapi:check                     in sync
  manual drift demo                      --check exits 1 on simulated drift

Checks passed:
  pnpm typecheck                         OK
  pnpm lint                              OK (340 files, no fixes)
  pnpm format:check                      OK (339 files; openapi.{yaml,json} excluded)
  pnpm test (workspace + scripts)        OK (893 + 21 = 914 tests)
  pnpm openapi:check                     OK (drift gate green; verified red on demo)

Known gaps:
  - Control-plane API OpenAPI is not yet generated. P6-000 (control-plane-api
    HTTP service) hasn't been built; when it lands, that task adds a sibling
    apps/control-plane-api/src/openapi/ module and a second docs/api/<name>.yaml.
  - Live /openapi.json is served unauthenticated. That's fine for the ingester
    (the doc is the public contract), but if any sensitive ops information ends
    up in the doc in future, this needs an auth gate.
  - The published `servers:` list points at placeholder hostnames. Production
    operators can override per-deployment via `openApiSetupWith({ servers })`,
    but the canonical YAML's `servers` field is intentionally a placeholder.
```

