# P0-006: Shared Schemas and Event Catalog

Status: Done

## Goal

Create the file-backed event catalog and code-backed Zod schemas for the first sample governed event.

## Required Reading

- [Event Contract](../../architecture/01-event-contract.md)
- [Claude Instructions](../../instructions/claude.md)
- [SDK Standards](../../architecture/10-sdk-standards.md)

## Dependencies

- P0-001
- P0-002

## Write Scope

Allowed:

```text
catalog/events/
packages/shared-schemas/
package.json
pnpm-workspace.yaml
```

Forbidden:

```text
apps/
processors/
consumers/
sql/
```

## Implementation Notes

- Use a rigid canonical envelope.
- Add one governed sample event, preferably `page.viewed` or `checkout.started`.
- Include an `experimental.*` handling path if simple, but do not overbuild.
- The ingester will later use this package for validation.
- Catalog YAML must include `lifecycle` field (`active`, `deprecated`) and an optional `sunset_at` for deprecated versions.
- Implement the schema evolution model from [Event Contract / Schema Evolution](../../architecture/01-event-contract.md): per-event integer `schema_version`, in-place vs version-bump rules, coexistence of versions, deprecation with sunset.
- Schema files are organized as `packages/shared-schemas/src/events/<domain>/<event>.v<N>.ts`. Multiple versions of the same event coexist as separate files.
- Catalog entries reference exact `schema_version` values; the loader exposes the union of active versions.
- Include a small evolution test that validates both an `active` and a `deprecated` example of the same event name, and rejects an event whose `schema_version` is past `sunset_at`.

## Acceptance Criteria

- [ ] Event catalog folder structure exists.
- [ ] At least one event catalog YAML exists.
- [ ] Matching Zod property schema exists.
- [ ] Envelope schema exists.
- [ ] Tests cover valid and invalid event examples.
- [ ] Catalog supports `lifecycle` and `sunset_at`.
- [ ] Loader returns versions per event with their lifecycle state.
- [ ] At least one event has both v1 (deprecated, with `sunset_at`) and v2 (active) fixtures demonstrating coexistence.
- [ ] Tests verify the ingester reason codes `unsupported_schema_version` and `schema_version_sunset` produce the right shape when emitted (codes only — emission lands in P2-003).

## Checks

Run where possible:

```text
pnpm typecheck
pnpm test
pnpm lint
```

## Handoff

```text
Files changed:
  catalog/events/page/viewed.v1.yaml        (new — deprecated, sunset_at 2026-08-10)
  catalog/events/page/viewed.v2.yaml        (new — active)
  catalog/events/checkout/started.v1.yaml   (new — active)
  packages/shared-schemas/package.json
  packages/shared-schemas/tsconfig.json
  packages/shared-schemas/tsconfig.build.json
  packages/shared-schemas/biome.json
  packages/shared-schemas/vitest.config.ts
  packages/shared-schemas/README.md
  packages/shared-schemas/src/index.ts
  packages/shared-schemas/src/reason-codes.ts
  packages/shared-schemas/src/envelope/primitives.ts
  packages/shared-schemas/src/envelope/envelope.ts
  packages/shared-schemas/src/envelope/index.ts
  packages/shared-schemas/src/events/page/viewed.v1.ts
  packages/shared-schemas/src/events/page/viewed.v2.ts
  packages/shared-schemas/src/events/checkout/started.v1.ts
  packages/shared-schemas/src/catalog/types.ts
  packages/shared-schemas/src/catalog/loader.ts
  packages/shared-schemas/src/catalog/bindings.ts
  packages/shared-schemas/src/catalog/validate.ts
  packages/shared-schemas/src/catalog/index.ts
  packages/shared-schemas/test/fixtures.ts
  packages/shared-schemas/test/envelope.test.ts
  packages/shared-schemas/test/events.test.ts
  packages/shared-schemas/test/catalog.test.ts
  packages/shared-schemas/test/validate.test.ts
  pnpm-lock.yaml                            (regenerated by pnpm install)

Commands run:
  pnpm install
  pnpm --filter @polaris/shared-schemas typecheck
  pnpm --filter @polaris/shared-schemas lint
  pnpm --filter @polaris/shared-schemas test
  pnpm --filter shared-schemas build
  pnpm typecheck
  pnpm lint
  pnpm test

Checks passed:
  pnpm typecheck                           PASS
  pnpm lint (biome check)                  PASS (0 issues across 18 files)
  pnpm test (vitest)                       PASS (43/43 tests, 4 files)
  pnpm --filter shared-schemas build       PASS (dist/ emitted, no YAML bundled)

Known gaps:
  - P0-002 (TypeScript tooling baseline) was listed as merged in the
    delegating prompt but had NOT actually been merged: no
    tsconfig.base.json, biome.json, vitest.config.ts, or root .gitignore
    entries existed in the worktree. To stay inside this task's write
    scope, the package was made self-contained: it ships its own
    standalone tsconfig.json (no `extends`), its own biome.json, and its
    own vitest.config.ts. When P0-002 lands, the package's tsconfig can
    be reduced to `extends: "../../tsconfig.base.json"` and the local
    biome/vitest configs collapsed; nothing else needs to change.
  - Root .gitignore currently lists only `resources/maxmind`. Generated
    artefacts (`node_modules/`, `dist/`, `coverage/`, `pnpm-lock.yaml`
    visibility) should be added by P0-001/P0-002 follow-up; this is
    outside the P0-006 write scope.
  - `experimental.*` handling: the loader treats experimental events
    exactly like governed events when they appear in the catalog. The
    task notes mention an experimental path is optional; deliberately
    deferred per "do not overbuild" guidance. The envelope, event-name
    regex, and reason codes are all already compatible.
  - Reason-code emission lands in P2-003. This package defines the
    closed set of codes (`unsupported_schema_version`,
    `schema_version_sunset`, plus three siblings) and the
    `schemaRejectionSchema` shape consumers can validate against.
  - Forbidden-field policy is intentionally not handled here; it lands
    with P0-009.
```

