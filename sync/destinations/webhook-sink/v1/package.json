{
  "name": "@polaris/consumer-webhook-sink-v1",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Polaris webhook-sink destination consumer v1. The simplest vendor: POST canonical events as JSON to a configurable HTTPS endpoint with an optional HMAC-SHA256 signature header. Serves as the canonical exemplar for vendor consumers (Meta CAPI, GA4, TikTok, Braze) landing later.",
  "license": "UNLICENSED",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "src",
    "consumer.manifest.yaml",
    "SPEC.md",
    "CHANGELOG.md"
  ],
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist coverage",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "biome lint src test",
    "format": "biome format --write src test",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "POLARIS_SERVICE_NAME=webhook-sink POLARIS_HTTP_PORT=5000 tsx watch src/main.ts",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@polaris/bus": "workspace:*",
    "@polaris/delivery-destinations": "workspace:*",
    "@polaris/delivery-host": "workspace:*",
    "@polaris/delivery-normalize": "workspace:*",
    "@polaris/destination-webhook-sink-v1": "workspace:*",
    "@polaris/observability-logger": "workspace:*",
    "@polaris/observability-metrics": "workspace:*",
    "@polaris/persistence-postgres": "workspace:*",
    "@polaris/runtime-config": "workspace:*",
    "@polaris/runtime-service-bootstrap": "workspace:*",
    "@polaris/tenancy-project-config": "workspace:*",
    "kysely": "^0.29.0",
    "zod": "^4.4.3"
  }
}
