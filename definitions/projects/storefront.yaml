# Catalog entry: project `storefront`.
#
# Projects are file-backed declarations materialized into PostgreSQL via
# `polaris projects sync`. Semantic membership stays in this file; the
# database row exists so FKs (sources, api_keys, audit records) can hang off
# a stable `project_id`.
project_id: storefront
display_name: Storefront
owner: storefront-platform
description: >-
  Customer-facing storefront across web, mobile, and backend services.
status: active

# Optional identity-stage overrides. Semantic input, loaded by
# `sync/identity/resolver` at boot and validated against the manifest's
# bounds — deliberately NOT materialized into PostgreSQL. Shape:
# `@polaris/governance` `projectIdentityOverrideSchema`.
#
# identity:
#   max_merges_per_window: 20
#   denylist:
#     customer_id:
#       - "guest"
#
# Optional enrichment-stage overrides, same contract — narrowing only,
# validated against `sync/enrichment/runtime`'s manifest bounds at boot.
#
# enrichment:
#   max_traits_bytes: 8192
