# Node / pnpm
node_modules/
.pnpm-store/

# Build output
dist/
build/
*.tsbuildinfo

# Test artifacts
coverage/
.vitest/

# OS / editor
.DS_Store
*.swp
Thumbs.db

# Environment
.env
.env.local
.env.*.local

# Claude Code internal state (worktrees, session metadata)
.claude/

# Project-specific
resources/maxmind
agents/

# Blueprints
blueprints/api-key
# The blueprint pins Turbopack's root at the repo root (see its next.config.ts),
# so running it writes a trace here as well as in its own .next/.
.next/

